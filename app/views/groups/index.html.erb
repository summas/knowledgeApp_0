<div class="container" style="background:#FFFFFF; width: 400px; padding: 10px;">
<h4 class="pt-3">
グループ管理画面</h4>
<span class="h6">※グループ一覧</span>
<table class="table table-bordered h6" style="background:#FFFFFF;">
	<thead class="thead-light">
		<tr>
			<th>Id</th><th>グループ名</th><th>編集</th><th>使用</th>
		</tr>
	</thead>
	<% @data.each do |obj| %>
	<tr>
		<td><%= obj.id %></td>
		<td><%= obj.name %></td>
		<td><a href="/groups/edit/<%= obj.id %>">編集</a></td>
		<% if !(obj.del_flg == DelFlg::STOP) %> 
			<td><%= link_to "停止", "/groups/stop/#{obj.id}", method: :patch, data: {confirm: "本当にグループ「#{obj.name}」を使用停止にしますか？"} %></td>
		<% else %>	
			<td><%= link_to "再開", "/groups/restart/#{obj.id}", method: :patch, data: {confirm: "本当にグループ「#{obj.name}」の使用を再開しますか？"} %></td>
		<% end %>	
	</tr>
	<% end %>
</table>
<span class="h6">※停止すると記事の登録や編集で該当グループは選択できなくなります。</span>
</div> 
<br>
<div class="container" style="background:#FFFFFF; width: 400px; padding: 10px;">	
<p>※新しいグループの登録</p>

<%= form_for(@group, url:{controller:'groups', action:'add'}) do |form| %>
<table>
    <div class="form-group">
		<td>グループ名：</td>
		<td><%= form.text_field :name, {class:"form-control"} %></td>
	</div>
</table>
	<div class="text-center">
	<%= form.submit "登録" %>
		<% end %>
	</div>
</div> 
<div class="text-center">
	<p><a href="/">戻る</a></p>
</div>
